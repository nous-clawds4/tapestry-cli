Word Type
=====

The concept of `concept headers` serves in the stead of what might otherwise be called the concept of `word types`. Where older versions of this protocol used `word type`, the current version uses `concept headers` in its place.
